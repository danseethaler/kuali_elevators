// Controller will make assignments to elevators
// All elevators will be available to the controller

function Controller(elevators) {
    this.elevators = elevators;
}

module.exports = Controller;
